const CheckingAuth = () => {
    return (
        <div className="page">
            <div className="h-full w-full flex justify-center items-center">
                <span className="w-20 h-20 animate-spin rounded-full border-2 border-transparent border-t-blue-700" />
            </div>
        </div>
    );
};

export default CheckingAuth;
